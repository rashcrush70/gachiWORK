// JavaScript for adding products to the cart
function addToCart(productId) {
    alert('Product ' + productId + ' added to cart!');
    // Add logic to handle the cart (e.g., updating a shopping cart object)
}
